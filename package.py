#In Python, a package is a way of organizing related modules into a single directory hierarchy. A package is simply a directory that contains a special file called __init__.py, which can be empty or can contain Python code to initialize the package. 
#The __init__.py file is used to define the package's namespace and can contain functions,
